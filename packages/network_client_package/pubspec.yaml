name: network_client_package
description: Connect to api
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^5.4.3+1
  dio_smart_retry: ^6.0.0
  equatable: ^2.0.3
  http: ^1.1.0
  meta: ^1.7.0
  pretty_dio_logger: ^1.3.1
  utils:
    path: ../utils
  task_master_storage:
    path: ../task_master_storage

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.16.0
